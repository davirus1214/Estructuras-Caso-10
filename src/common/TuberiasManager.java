package common;

public class TuberiasManager {
	
	
	/*agregar un sensor o toma de agua, es casi que llamar al metodo que está en el 
	 * arbol para que lo agregue
	Un metodo de revisarConsumo, dicho metodo lo que hace es ir recursivamente, 
	digamos en preorden, sumando y revisando el consumo actual de cada punto, y alertar si en algun punto ya no alcanza el agua, retornando el nodo donde encontraste que se reportó el faltante, esto es que la suma del consumo ya supera la capacidad de la planta, el metodo podria recibir el nodo raiz donde va iniciar a buscar y el madximo que debe sumar, y luego el metodo se puede llamar a si mismo recursivamente. 
	
	Hace esto y me lo pasas a whatsapp para revisarlo. 
*/
	
	
	public void agregarSensor() {
		
		
	}
	
}
